#pragma once

// Build configuration generated by CMake
#ifndef LFS_VULKAN_VIEWER_ENABLED
#cmakedefine LFS_VULKAN_VIEWER_ENABLED
#endif

// Version information
#define PROJECT_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define PROJECT_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define PROJECT_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define PROJECT_VERSION_STRING "@PROJECT_VERSION@"

// Git version (generated at build time)
#include "git_version.h"

// Build type
#cmakedefine DEBUG_BUILD
#cmakedefine RELEASE_BUILD

// Platform detection
#if defined(_WIN32) || defined(_WIN64)
#define PLATFORM_WINDOWS
#elif defined(__linux__)
#define PLATFORM_LINUX
#else
#define PLATFORM_UNKNOWN
#endif

// Feature flags
#ifdef LFS_VULKAN_VIEWER_ENABLED
#define USE_VULKAN_VIEWER 1
#else
#define USE_VULKAN_VIEWER 0
#endif
